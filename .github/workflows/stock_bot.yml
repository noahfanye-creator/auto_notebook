name: 股票分析机器人 🤖

# ============================================================================
# GitHub Repository Secrets 配置说明
# ============================================================================
# 请在 GitHub Repository Settings > Secrets and variables > Actions 中配置以下变量：
#
# 1. TELEGRAM_BOT_TOKEN
#    说明: Telegram Bot Token
#    获取方式: 通过 @BotFather 创建机器人后获取
#    示例: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
#
# 2. TELEGRAM_CHAT_ID
#    说明: Telegram Chat ID（接收消息的聊天ID）
#    获取方式: 通过 @userinfobot 或 @getidsbot 获取
#    示例: 123456789
#
# 3. GDRIVE_CREDENTIALS
#    说明: Google Drive 服务账号 JSON 凭证，存原始 JSON 全文（勿 base64）
#    获取方式:
#       a) 在 Google Cloud Console 创建服务账号，下载 JSON 密钥文件
#       b) 用编辑器打开 credentials.json，复制整个内容
#       c) 粘贴到 GDRIVE_CREDENTIALS，保持合法 JSON（可含换行）
#    示例: {"type":"service_account","project_id":"..."}
#
# 4. GDRIVE_FOLDER_ID
#    说明: Google Drive 目标文件夹 ID（必须为「协作云端硬盘」内文件夹）
#    获取方式: 在 Drive 中打开该文件夹，从 URL 的 folders/xxx 提取 ID
#    示例: 1a2b3c4d5e6f7g8h9i0j
#
# 注意事项:
# - GDRIVE_CREDENTIALS 存原始 JSON，不要 base64 编码
# - 服务账号无存储配额，须用「协作云端硬盘」(Shared Drive)：新建协作云端硬盘 →
#   在盘内建文件夹 → 右键该文件夹「管理成员」添加 client_email 为「内容管理员」→
#   用该文件夹 ID 作为 GDRIVE_FOLDER_ID。勿用「我的云端硬盘」普通文件夹，否则 403。
# - 确保已启用 Google Drive API
# ============================================================================

on:
  schedule:
    # 交易日午盘与收盘（北京时间）
    # A股：11:30 / 15:00；港股：12:00 / 16:00
    # 触发时间加 5 分钟缓冲
    - cron: '35 3 * * 1-5'  # 11:35
    - cron: '5 4 * * 1-5'   # 12:05
    - cron: '5 7 * * 1-5'   # 15:05
    - cron: '10 8 * * 1-5'  # 16:10
  workflow_dispatch:
    inputs:
      stocks:
        description: '输入股票代码（支持A股和港股，如：600460,00700）'
        required: false
        default: '688630,600460,300474,300623,300019'

jobs:
  analyze-stocks:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-core.txt -r requirements-optional.txt
        pip install pytz
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei

    - name: 运行股票分析
      id: run_analysis
      timeout-minutes: 25
      continue-on-error: true
      env:
        DATABASE_ENABLED: "false"
      run: |
        # 临时禁用数据库：报告只用接口实时数据，不读不写本地 DB
        export TZ='Asia/Shanghai'
        # 记录运行开始时间
        export RUN_START_TIME=$(date +%s)
        echo "RUN_START_TIME=$RUN_START_TIME" >> $GITHUB_ENV
        echo "运行开始时间: $(date) (Timestamp: $RUN_START_TIME)"
        
        STOCKS="${{ github.event.inputs.stocks }}"
        if [ -z "$STOCKS" ]; then
          STOCKS="688630,600460,300474,300623,300019"
        fi
        STOCKS="${STOCKS//,/ }"
        STOCKS="${STOCKS//，/ }"
        STOCKS="$(echo "$STOCKS" | xargs)"
        # 使用 manual 模式，这样脚本只管生成 PDF，不会报错也不会重复发消息
        python github_stock_bot.py --mode manual --stocks "$STOCKS"
        
        # 检查是否有PDF文件生成（直接统计PDF文件）
        PDF_COUNT=$(python3 -c "
        import os
        import glob
        run_start = int(os.environ.get('RUN_START_TIME', 0))
        # 查找最新报告文件夹
        report_dirs = glob.glob('reports/reports_*')
        if not report_dirs:
            print(0)
            exit()
        latest_dir = max(report_dirs, key=os.path.getmtime)
        # 只统计在运行开始后创建的PDF文件（允许60秒误差）
        pdf_paths = glob.glob(os.path.join(latest_dir, '*.pdf'))
        pdf_paths = [p for p in pdf_paths if os.path.isfile(p) and os.path.getmtime(p) >= run_start - 60]
        print(len(pdf_paths))
        ")
        echo "本次运行生成的PDF数量: $PDF_COUNT"
        echo "PDF_COUNT=$PDF_COUNT" >> $GITHUB_OUTPUT
        
        # 保存报告目录路径供后续步骤使用
        REPORT_DIR=$(python3 -c "
        import os
        import glob
        run_start = int(os.environ.get('RUN_START_TIME', 0))
        report_dirs = glob.glob('reports/reports_*')
        if report_dirs:
            latest_dir = max(report_dirs, key=os.path.getmtime)
            print(os.path.abspath(latest_dir))
        ")
        echo "REPORT_DIR=$REPORT_DIR" >> $GITHUB_ENV
        echo "报告目录: $REPORT_DIR"
    
    - name: 发送报告到 Telegram（多文件推送）🚀
      if: steps.run_analysis.outcome == 'success' && steps.run_analysis.outputs.PDF_COUNT > 0
      run: |
        REPORT_DIR="${{ env.REPORT_DIR }}"
        if [ -z "$REPORT_DIR" ] || [ ! -d "$REPORT_DIR" ]; then
          echo "❌ 报告目录不存在: $REPORT_DIR"
          exit 1
        fi
        
        TELEGRAM_TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}"
        TELEGRAM_CHAT_ID="${{ secrets.TELEGRAM_CHAT_ID }}"
        
        if [ -z "$TELEGRAM_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
          echo "❌ Telegram配置缺失，请检查secrets: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID"
          exit 1
        fi
        
        # 统计PDF文件数量
        PDF_FILES=("$REPORT_DIR"/*.pdf)
        PDF_COUNT=${#PDF_FILES[@]}
        
        if [ $PDF_COUNT -eq 0 ]; then
          echo "⚠️ 未找到PDF文件"
          exit 1
        fi
        
        echo "📤 开始发送 $PDF_COUNT 个PDF文件到Telegram..."
        
        SUCCESS_COUNT=0
        FAIL_COUNT=0
        
        # 循环发送每个PDF文件
        for pdf_file in "$REPORT_DIR"/*.pdf; do
          if [ ! -f "$pdf_file" ]; then
            continue
          fi
          
          FILENAME=$(basename "$pdf_file")
          FILE_SIZE=$(stat -f%z "$pdf_file" 2>/dev/null || stat -c%s "$pdf_file" 2>/dev/null || echo "0")
          FILE_SIZE_MB=$((FILE_SIZE / 1024 / 1024))
          
          # Telegram Bot API 文件大小限制为 50MB
          if [ $FILE_SIZE_MB -gt 50 ]; then
            echo "⚠️ 跳过文件 $FILENAME (大小: ${FILE_SIZE_MB}MB，超过50MB限制)"
            FAIL_COUNT=$((FAIL_COUNT + 1))
            continue
          fi
          
          echo "📄 正在发送: $FILENAME (${FILE_SIZE_MB}MB)..."
          
          # 发送文件到Telegram
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            -F "chat_id=$TELEGRAM_CHAT_ID" \
            -F "document=@$pdf_file" \
            "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendDocument" 2>&1)
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ 成功发送: $FILENAME"
            SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
          else
            echo "❌ 发送失败: $FILENAME (HTTP $HTTP_CODE)"
            echo "响应: $BODY"
            FAIL_COUNT=$((FAIL_COUNT + 1))
          fi
          
          # 避免请求过快，添加短暂延迟
          sleep 1
        done
        
        echo ""
        echo "📊 发送完成统计:"
        echo "  ✅ 成功: $SUCCESS_COUNT"
        echo "  ❌ 失败: $FAIL_COUNT"
        echo "  📄 总计: $PDF_COUNT"
        
        # 发送完成通知
        if [ $SUCCESS_COUNT -gt 0 ]; then
          curl -s -X POST \
            -F "chat_id=$TELEGRAM_CHAT_ID" \
            -F "text=✅ Felix，机器人已完成股票分析报告推送（成功: $SUCCESS_COUNT/$PDF_COUNT）" \
            "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" > /dev/null
        fi
        
        # 如果有失败的文件，退出码为1
        if [ $FAIL_COUNT -gt 0 ]; then
          exit 1
        fi

    - name: 上传报告到 Google Drive 📁
      if: steps.run_analysis.outcome == 'success' && steps.run_analysis.outputs.PDF_COUNT > 0
      env:
        GDRIVE_CREDENTIALS: ${{ secrets.GDRIVE_CREDENTIALS }}
        GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
        REPORT_DIR: ${{ env.REPORT_DIR }}
      run: |
        pip install -q google-auth google-api-python-client
        python - << 'PY'
        import os
        import json
        import glob
        from google.oauth2.service_account import Credentials
        from googleapiclient.discovery import build
        from googleapiclient.http import MediaFileUpload

        report_dir = os.environ["REPORT_DIR"]
        raw = os.environ.get("GDRIVE_CREDENTIALS", "").strip()
        folder_id = os.environ.get("GDRIVE_FOLDER_ID", "").strip()
        if not raw or not folder_id:
            raise SystemExit("缺少 GDRIVE_CREDENTIALS 或 GDRIVE_FOLDER_ID")

        creds_dict = json.loads(raw)
        creds = Credentials.from_service_account_info(creds_dict, scopes=["https://www.googleapis.com/auth/drive"])
        drive = build("drive", "v3", credentials=creds)
        sa_email = creds_dict.get("client_email", "未知")

        # 上传前校验：文件夹是否存在且已与服务账号共享（支持协作云端硬盘）
        try:
            drive.files().get(fileId=folder_id, fields="id,name", supportsAllDrives=True).execute()
        except Exception as e:
            err = str(e)
            if "404" in err or "notFound" in err or "not found" in err.lower():
                raise SystemExit(
                    "Google Drive 文件夹不存在或未与服务账号共享。\n"
                    "请：1) 确认 GDRIVE_FOLDER_ID 正确；2) 在 Drive 中右键该文件夹 → 共享 → "
                    f"添加此邮箱并设为「编辑者」：\n  {sa_email}"
                ) from e
            raise

        pdfs = sorted(glob.glob(os.path.join(report_dir, "*.pdf")))
        if not pdfs:
            raise SystemExit("未找到 PDF 文件")

        from googleapiclient.errors import HttpError

        for path in pdfs:
            name = os.path.basename(path)
            meta = {"name": name, "parents": [folder_id]}
            assert "parents" in meta and len(meta["parents"]) > 0, "上传须指定 parents（目标文件夹 ID）"
            media = MediaFileUpload(path, mimetype="application/pdf", resumable=True)
            try:
                f = drive.files().create(body=meta, media_body=media, fields="id,name", supportsAllDrives=True).execute()
                print(f"  ✅ {name} -> {f.get('id')}")
            except HttpError as e:
                if e.resp.status == 404 and "fileId" in str(e):
                    raise SystemExit(
                        "上传失败：目标文件夹不可访问 (404)。请确认：\n"
                        "1) GDRIVE_FOLDER_ID 正确；2) 已将文件夹共享给服务账号：\n  "
                        f"{sa_email}\n（右键文件夹 → 共享 → 添加上述邮箱为编辑者）"
                    ) from e
                if e.resp.status == 403 and ("storageQuotaExceeded" in str(e) or "storage quota" in str(e).lower()):
                    raise SystemExit(
                        "上传失败：服务账号无存储配额 (403)。请使用「协作云端硬盘」：\n"
                        "1) 新建协作云端硬盘；2) 在盘内建文件夹；3) 管理成员添加 "
                        f"{sa_email} 为「内容管理员」；4) 用该文件夹 ID 作为 GDRIVE_FOLDER_ID"
                    ) from e
                raise
        print(f"共上传 {len(pdfs)} 个文件")
        PY

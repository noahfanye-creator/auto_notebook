# 依赖管理说明
# 
# 本项目已将依赖拆分为三个文件：
# 1. requirements-core.txt  - 核心依赖（必需）
# 2. requirements-optional.txt - 可选依赖（增强功能）
# 3. requirements-dev.txt - 开发依赖（仅开发/测试需要）
#
# 快速安装：
#   pip install -r requirements-core.txt
#   pip install -r requirements-optional.txt  # 可选
#   pip install -r requirements-dev.txt  # 仅开发时
#
# 或安装所有依赖：
#   pip install -r requirements-core.txt -r requirements-optional.txt
#
# 本文件保留用于向后兼容，建议使用上述分类文件

# 核心依赖
-r requirements-core.txt

# 可选依赖（取消注释以安装）
# -r requirements-optional.txt

# 开发依赖（仅开发时使用）
# -r requirements-dev.txt

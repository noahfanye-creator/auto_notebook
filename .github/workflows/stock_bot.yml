name: 股票分析机器人 🤖

on:
  schedule:
    - cron: '15 1 * * 1-5'
    - cron: '0 4 * * 1-5'
    - cron: '0 7 * * 1-5'
  workflow_dispatch:
    inputs:
      stocks:
        description: '输入股票代码'
        required: false
        default: '688630,600460,300474,300623,300019'

jobs:
  analyze-stocks:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
    
    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas numpy reportlab akshare matplotlib pytz
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei
    
    - name: 运行股票分析
      # 这里注意：我把 mode 改回了 manual，这是你脚本支持的
      run: |
        export TZ='Asia/Shanghai'
        STOCKS="${{ github.event.inputs.stocks }}"
        if [ -z "$STOCKS" ]; then
          STOCKS="688630,600460,300474,300623,300019"
        fi
        # 使用 manual 模式，这样脚本只管生成 PDF，不会报错也不会重复发消息
        python github_stock_bot.py --mode manual --stocks "$STOCKS"
    
    - name: 整理报告文件
      run: |
        mkdir -p final_delivery
        # 你的脚本会生成 reports/ 文件夹，我们把里面的 zip 找出来
        find reports -name "*.zip" -exec cp {} final_delivery/stock_report.zip \;

    - name: 上传报告到 GitHub Artifact
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: stock-reports-${{ github.run_id }}
        path: |
          final_delivery/stock_report.zip

    - name: 发送报告到 Telegram 🚀
      if: success()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        message: "✅ Felix，机器人已完成股票分析报告\n📥 下载链接（Actions Artifact）: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

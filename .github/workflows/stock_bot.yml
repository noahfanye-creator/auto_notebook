name: 股票分析机器人 🤖

on:
  schedule:
    # 交易日午盘与收盘（北京时间）
    # A股：11:30 / 15:00；港股：12:00 / 16:00
    # 触发时间加 5 分钟缓冲
    - cron: '35 3 * * 1-5'  # 11:35
    - cron: '5 4 * * 1-5'   # 12:05
    - cron: '5 7 * * 1-5'   # 15:05
    - cron: '10 8 * * 1-5'  # 16:10
  workflow_dispatch:
    inputs:
      stocks:
        description: '输入股票代码（支持A股和港股，如：600460,00700）'
        required: false
        default: '688630,600460,300474,300623,300019'

jobs:
  analyze-stocks:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
    
    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas numpy reportlab akshare matplotlib pytz yfinance
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei
    
    - name: 运行股票分析
      id: run_analysis
      run: |
        export TZ='Asia/Shanghai'
        # 记录运行开始时间
        RUN_START_TIME=$(date +%s)
        echo "RUN_START_TIME=$RUN_START_TIME" >> $GITHUB_ENV
        echo "运行开始时间: $(date) (Timestamp: $RUN_START_TIME)"
        
        STOCKS="${{ github.event.inputs.stocks }}"
        if [ -z "$STOCKS" ]; then
          STOCKS="688630,600460,300474,300623,300019"
        fi
        STOCKS="${STOCKS//,/ }"
        STOCKS="${STOCKS//，/ }"
        STOCKS="$(echo "$STOCKS" | xargs)"
        # 使用 manual 模式，这样脚本只管生成 PDF，不会报错也不会重复发消息
        python github_stock_bot.py --mode manual --stocks "$STOCKS"
        
        # 检查是否有PDF文件生成（使用时间戳比较）
        PDF_COUNT=$(python3 -c "
        import os
        import glob
        run_start = int('$RUN_START_TIME')
        pdfs = glob.glob('reports/**/*.pdf', recursive=True)
        pdfs = [p for p in pdfs if os.path.isfile(p) and os.path.getmtime(p) >= run_start - 60]
        print(len(pdfs))
        ")
        echo "本次运行生成的PDF数量: $PDF_COUNT"
        echo "PDF_COUNT=$PDF_COUNT" >> $GITHUB_OUTPUT
    
    - name: 整理报告文件
      if: steps.run_analysis.outputs.PDF_COUNT > 0
      run: |
        mkdir -p final_delivery
        
        # 等待一下，确保ZIP文件有时间生成
        sleep 2
        
        # 选择本次运行生成的最新 ZIP（创建时间在运行开始之后）
        ZIP_PATH=$(python -c "
        import glob
        import os
        import sys
        run_start = int('${{ env.RUN_START_TIME }}')
        paths = glob.glob('reports/**/stock_reports_*.zip', recursive=True)
        paths = [p for p in paths if os.path.isfile(p)]
        # 只选择在运行开始后创建的ZIP文件（允许60秒误差）
        paths = [p for p in paths if os.path.getmtime(p) >= run_start - 60]
        if paths:
            paths.sort(key=lambda p: os.path.getmtime(p), reverse=True)
            print(paths[0])
        ")
        
        if [ -z "$ZIP_PATH" ] || [ ! -f "$ZIP_PATH" ]; then
          echo "❌ 未找到本次运行生成的报告 ZIP"
          echo "检查 reports 目录:"
          ls -la reports/*/stock_reports_*.zip 2>/dev/null || echo "未找到ZIP文件"
          echo "运行开始时间戳: ${{ env.RUN_START_TIME }}"
          echo "当前时间戳: $(date +%s)"
          exit 1
        fi
        
        # 验证ZIP文件大小（至少应该有一些内容）
        ZIP_SIZE=$(stat -f%z "$ZIP_PATH" 2>/dev/null || stat -c%s "$ZIP_PATH" 2>/dev/null || echo "0")
        if [ "$ZIP_SIZE" -lt 1000 ]; then
          echo "⚠️ ZIP文件太小 ($ZIP_SIZE 字节)，可能不完整"
          exit 1
        fi
        
        echo "✅ 使用报告ZIP: $ZIP_PATH (大小: $ZIP_SIZE 字节)"
        cp "$ZIP_PATH" final_delivery/stock_report.zip

    - name: 上传报告到 GitHub Artifact
      if: steps.run_analysis.outputs.PDF_COUNT > 0
      uses: actions/upload-artifact@v4
      with:
        name: stock-reports-${{ github.run_id }}
        path: |
          final_delivery/stock_report.zip

    - name: 发送报告到 Telegram 🚀
      if: steps.run_analysis.outputs.PDF_COUNT > 0
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        document: final_delivery/stock_report.zip
        message: "✅ Felix，机器人已完成股票分析报告（ZIP）"

name: 股票分析机器人 🤖

on:
  workflow_dispatch:
    inputs:
      stocks:
        description: '股票代码（空格分隔）'
        required: false
        default: '600460 300474 300623 300420'
        type: string
  schedule:
    # 北京时间周一至周五 15:10 自动运行一次（收盘后）
    - cron: '10 7 * * 1-5'

jobs:
  analyze-stocks:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
    
    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas numpy reportlab akshare matplotlib pytz
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei
    
    - name: 运行股票分析
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        export TZ='Asia/Shanghai'
        # 运行你上传的增强版脚本
        python enhanced_stock_analysis_system.py --mode telegram --stocks '${{ github.event.inputs.stocks }}'
    
    - name: 上传报告到 GitHub (备用下载)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: stock-reports-${{ github.run_id }}
        path: |
          **/reports_*/*.pdf
          **/reports_*/*.zip

    - name: 发送报告到 Telegram 🚀
      if: success()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        document: reports/**/*.zip
        caption: "✅ Felix，报告已生成！\n📈 包含：士兰微、景嘉微、捷捷微电、五洋自控\n📊 指标：MACD, RSI, KDJ, 威廉, OBV"

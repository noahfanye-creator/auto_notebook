- name: 上传分析报告
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: stock-reports-${{ github.run_id }}
        # 修正路径：直接匹配 reports 文件夹下的所有内容
        path: |
          reports/**/*.pdf
          reports/**/*.zip

# 🔄 代码重构待办清单

> 创建时间：2026-01-25  
> 目标：逐步重构代码，每完成一步调试成功后再进行下一步

---

## 📋 任务列表

### ✅ 阶段一：基础准备（低风险，快速完成）

#### [x] 任务 1：创建测试框架和基础结构
**优先级**：🔴 高  
**预计时间**：30 分钟  
**依赖**：无

**任务内容**：
- [x] 安装 pytest：`pip install pytest pytest-cov`
- [x] 创建 `tests/` 目录结构
- [x] 创建 `tests/__init__.py`
- [x] 创建 `tests/conftest.py`（共享 fixtures）
- [x] 创建第一个测试文件 `tests/test_utils/test_code_normalizer.py`
- [x] 运行测试验证框架工作正常：`pytest tests/ -v`

**验收标准**：
- ✅ pytest 可以正常运行
- ✅ 测试目录结构创建完成
- ✅ 可以运行空测试通过

**调试检查点**：
```bash
# 检查 pytest 安装
pytest --version

# 运行测试
pytest tests/ -v
```

---

### ✅ 阶段二：工具函数提取（低风险，独立模块）

#### [x] 任务 2：提取工具函数模块
**优先级**：🔴 高  
**预计时间**：1-2 小时  
**依赖**：任务 1

**任务内容**：
- [x] 创建 `src/utils/code_normalizer.py`
  - [x] 迁移 `normalize_code()` 函数
  - [x] 迁移 `is_hk_stock()` 函数
  - [x] 迁移 `normalize_hk_code()` 函数
  - [x] 迁移 `parse_stock_list()` 函数
- [x] 创建 `src/utils/trading_hours.py`
  - [x] 迁移 `is_china_stock_market_open()` 函数
  - [x] 迁移 `is_hk_stock_market_open()` 函数
- [x] 创建 `src/utils/font_setup.py`
  - [x] 迁移 `setup_fonts()` 函数
- [x] 在主文件中更新导入语句
- [x] 运行程序验证功能正常

**验收标准**：
- ✅ 所有工具函数可以正常导入
- ✅ 原有功能保持不变
- ✅ 运行 `python github_stock_bot.py --mode manual --stocks "600460"` 成功

**调试检查点**：
```python
# 测试导入
from src.utils.code_normalizer import normalize_code, is_hk_stock
from src.utils.trading_hours import is_china_stock_market_open
from src.utils.font_setup import setup_fonts

# 测试功能
assert normalize_code("600460") == "sh600460"
assert is_hk_stock("00700") == True
```

---

#### [x] 任务 3：提取数据获取模块
**优先级**：🟡 中  
**预计时间**：2-3 小时  
**依赖**：任务 2

**任务内容**：
- [x] 创建 `src/data/fetchers/__init__.py`
- [x] 创建 `src/data/fetchers/a_share_fetcher.py`
  - [x] 迁移 `fetch_kline_data_from_sina()` 函数
  - [x] 迁移 `fetch_kline_data_fallback()` 函数
  - [x] 迁移 `fetch_kline_data()` 函数（A股部分）
- [x] 创建 `src/data/fetchers/hk_stock_fetcher.py`
  - [x] 迁移 `fetch_kline_data_from_hk_sources()` 函数
  - [x] 迁移 `fetch_alternative_1min_data()` 函数
- [x] 创建 `src/data/fetchers/sector_fetcher.py`
  - [x] 迁移行业相关数据获取函数
- [x] 在主文件中更新导入语句
- [x] 运行程序验证数据获取正常

**验收标准**：
- ✅ A股数据获取正常
- ✅ 港股数据获取正常
- ✅ 行业数据获取正常
- ✅ 运行完整分析流程成功

**调试检查点**：
```python
# 测试 A股数据
from src.data.fetchers.a_share_fetcher import fetch_kline_data
df = fetch_kline_data("sh600460", scale=240, datalen=100)
assert not df.empty

# 测试港股数据
from src.data.fetchers.hk_stock_fetcher import fetch_kline_data_from_hk_sources
df = fetch_kline_data_from_hk_sources("HK.00700", scale=240, datalen=100)
assert not df.empty
```

---

### ✅ 阶段三：分析模块提取（中等风险）

#### [x] 任务 4：提取技术指标计算
**优先级**：🟡 中  
**预计时间**：2-3 小时  
**依赖**：任务 3

**任务内容**：
- [x] 创建 `src/analysis/indicators.py`
  - [x] 迁移 `calculate_technical_indicators()` 函数
  - [x] 迁移所有指标计算相关辅助函数
- [x] 创建 `src/analysis/market_analyzer.py`
  - [x] 迁移 `get_market_indices_data()` 函数
  - [x] 迁移 `get_sector_indices_data()` 函数
  - [x] 迁移 `get_market_summary_analysis()` 函数
  - [x] 迁移 `get_market_sentiment_analysis()` 函数
- [x] 在主文件中更新导入语句
- [x] 运行程序验证指标计算正常

**验收标准**：
- ✅ 所有技术指标计算正确
- ✅ 市场分析功能正常
- ✅ PDF 报告中的指标数据正确

**调试检查点**：
```python
# 测试指标计算
from src.analysis.indicators import calculate_technical_indicators
df_with_indicators = calculate_technical_indicators(df)
assert 'MA5' in df_with_indicators.columns
assert 'RSI' in df_with_indicators.columns
assert 'MACD' in df_with_indicators.columns
```

---

#### [x] 任务 5：提取 PDF 生成模块
**优先级**：🟡 中  
**预计时间**：3-4 小时  
**依赖**：任务 4

**任务内容**：
- [x] 创建 `src/visualization/pdf_generator.py`
  - [x] 迁移 `create_pdf_with_market_analysis()` 函数
  - [x] 迁移 `create_candle_chart()` 函数
  - [x] 迁移 `create_indices_charts()` 函数
  - [x] 迁移所有 PDF 生成辅助函数
- [x] 创建 `src/visualization/report_templates.py`
  - [x] 提取报告模板相关函数
- [x] 在主文件中更新导入语句
- [x] 运行程序验证 PDF 生成正常

**验收标准**：
- ✅ PDF 文件可以正常生成
- ✅ PDF 内容完整（包含所有图表和表格）
- ✅ PDF 格式正确（字体、布局）

**调试检查点**：
```python
# 测试 PDF 生成
from src.visualization.pdf_generator import create_pdf_with_market_analysis
result = create_pdf_with_market_analysis(
    stock_code="sh600460",
    stock_name="测试股票",
    stock_data_map={...},
    indices_data={...},
    save_path="test_report.pdf",
    temp_dir="temp"
)
assert result == True
assert os.path.exists("test_report.pdf")
```

---

### ✅ 阶段四：主文件重构（高风险，需要仔细测试）

#### [x] 任务 6：重构主文件
**优先级**：🔴 高  
**预计时间**：2-3 小时  
**依赖**：任务 2-5 全部完成

**任务内容**：
- [x] 创建 `src/report/generator.py`
  - [x] 迁移 `process_multiple_stocks()` 函数
  - [x] 迁移 `create_zip_archive()` 函数
- [x] 简化 `github_stock_bot.py`
  - [x] 只保留命令行参数解析
  - [x] 只保留主流程调用
  - [x] 目标：主文件 < 200 行
- [x] 更新所有导入语句
- [x] 运行完整流程验证

**验收标准**：
- ✅ 主文件代码行数 < 200
- ✅ 所有功能正常工作
- ✅ 命令行接口保持不变
- ✅ GitHub Actions 可以正常运行

**调试检查点**：
```bash
# 测试命令行接口
python github_stock_bot.py --mode manual --stocks "600460 300474"

# 测试完整流程
python github_stock_bot.py --mode manual --stocks "600460" --sector "光伏设备"

# 检查主文件行数
wc -l github_stock_bot.py  # 应该 < 200
```

---

### ✅ 阶段五：配置和基础设施（低风险）

#### [x] 任务 7：添加配置管理
**优先级**：🟡 中  
**预计时间**：1-2 小时  
**依赖**：任务 6

**任务内容**：
- [x] 创建 `config/config.yaml`（基于 config.example.yaml）
- [x] 创建 `src/config/loader.py`
  - [x] 实现配置加载类
  - [x] 支持环境变量覆盖
- [x] 替换硬编码的 `TARGET_STOCKS`
- [x] 替换其他硬编码配置
- [x] 运行程序验证配置加载正常

**验收标准**：
- ✅ 配置可以从 YAML 文件加载
- ✅ 环境变量可以覆盖配置
- ✅ 默认配置工作正常

**调试检查点**：
```python
# 测试配置加载
from src.config.loader import Config
config = Config()
stocks = config.stocks
assert isinstance(stocks, list)
assert len(stocks) > 0
```

---

#### [x] 任务 8：添加日志系统
**优先级**：🟡 中  
**预计时间**：1-2 小时  
**依赖**：任务 7

**任务内容**：
- [x] 创建 `src/utils/logger.py`
  - [x] 实现结构化日志设置
  - [x] 支持文件和控制台输出
- [x] 替换关键位置的 `print()` 语句
- [x] 添加不同级别的日志（DEBUG, INFO, WARNING, ERROR）
- [x] 运行程序验证日志输出正常

**验收标准**：
- ✅ 日志文件可以正常生成
- ✅ 日志格式清晰易读
- ✅ 不同级别日志正确输出

**调试检查点**：
```python
# 测试日志
from src.utils.logger import setup_logger
logger = setup_logger(__name__, 'test.log')
logger.info("测试日志")
logger.error("测试错误日志")
# 检查 logs/test.log 文件
```

---

### ✅ 阶段六：性能优化（可选，提升体验）

#### [x] 任务 9：添加数据缓存
**优先级**：🟢 低  
**预计时间**：2-3 小时  
**依赖**：任务 8

**任务内容**：
- [x] 创建 `src/utils/cache.py`
  - [x] 实现文件缓存机制
  - [x] 支持 TTL（生存时间）
- [x] 在数据获取函数中集成缓存
- [x] 运行程序验证缓存工作正常

**验收标准**：
- ✅ 相同数据请求使用缓存
- ✅ 缓存过期后重新获取
- ✅ 缓存文件可以正常读写

---

#### [x] 任务 10：添加并发处理
**优先级**：🟢 低  
**预计时间**：2-3 小时  
**依赖**：任务 9

**任务内容**：
- [x] 创建 `src/utils/parallel.py`
  - [x] 实现并发处理函数
- [x] 在批量处理中应用并发
- [x] 运行程序验证并发处理正常

**验收标准**：
- ✅ 批量处理速度明显提升
- ✅ 并发处理结果正确
- ✅ 错误处理正常

---

### ✅ 阶段七：测试和文档（提升质量）

#### [x] 任务 11：编写单元测试
**优先级**：🟡 中  
**预计时间**：4-6 小时  
**依赖**：任务 2-6

**任务内容**：
- [x] 为工具函数编写测试
- [x] 为数据获取函数编写测试（使用 mock）
- [x] 为指标计算函数编写测试
- [x] 运行测试套件，确保覆盖率 > 60%

**验收标准**：
- ✅ 核心功能都有测试覆盖
- ✅ 测试通过率 100%
- ✅ 测试覆盖率 > 60%

---

#### [x] 任务 12：优化依赖管理
**优先级**：🟢 低  
**预计时间**：1 小时  
**依赖**：无

**任务内容**：
- [x] 拆分 `requirements.txt`
  - [x] `requirements-core.txt` - 核心依赖
  - [x] `requirements-optional.txt` - 可选依赖
  - [x] `requirements-dev.txt` - 开发依赖
- [x] 固定版本号
- [x] 更新 README 说明
- [x] 创建依赖管理文档

**验收标准**：
- ✅ 依赖文件清晰分类
- ✅ 版本号固定
- ✅ 安装说明更新

---

### 完成统计
- 总任务数：12
- 已完成：12
- 进行中：0
- 待开始：0

---

## 📊 进度跟踪

### 当前阶段
- [x] 阶段一：基础准备
- [x] 阶段二：工具函数提取
- [x] 阶段三：分析模块提取
- [x] 阶段四：主文件重构
- [x] 阶段五：配置和基础设施
- [x] 阶段六：性能优化
- [x] 阶段七：测试和文档

---

## 🎯 工作流程

### 每完成一个任务后：

1. **运行测试**：
   ```bash
   # 运行单元测试
   pytest tests/ -v
   
   # 运行完整流程测试
   python github_stock_bot.py --mode manual --stocks "600460"
   ```

2. **检查功能**：
   - ✅ 原有功能正常工作
   - ✅ 新模块可以正常导入
   - ✅ 没有引入新的错误

3. **提交代码**：
   ```bash
   git add .
   git commit -m "feat: 完成任务 X - [任务描述]"
   ```

4. **更新待办清单**：
   - 在任务前打勾 [x]
   - 更新完成统计

5. **开始下一个任务**

---

## ⚠️ 注意事项

1. **不要跳过任务**：每个任务都有依赖关系
2. **充分测试**：每完成一个任务都要测试
3. **保持兼容**：确保原有功能不受影响
4. **小步提交**：每完成一个任务就提交代码
5. **遇到问题及时回滚**：如果某个任务导致问题，及时回退

---

## ✅ 优化已完成

**所有 12 项任务均已完成。** 日常维护建议：

- 运行测试：`pytest tests/ -v`
- 完整流程：`python github_stock_bot.py --mode manual --stocks "600460"`
- 持续补充单元测试、完善 API 文档（参见 OPTIMIZATION_RECOMMENDATIONS.md 低优先级建议）
